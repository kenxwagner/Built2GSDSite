//Pull up page displaying a schedule.
// decision of array to display then use a 
// better optimization with a different data structure. 
function getServer(){
	if (serverOn === 'true') {
	continue;
	} else {
	console.log("Server not available")
	}
	
}
//Default is user schedule.

